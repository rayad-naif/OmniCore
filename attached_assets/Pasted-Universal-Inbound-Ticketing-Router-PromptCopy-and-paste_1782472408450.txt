Universal Inbound Ticketing Router" PromptCopy and paste this exactly into your Replit AI Agent:"Role: Senior Code Executor.Strict Rule: DO NOT PLAN. DO NOT OUTPUT LOGS. Execute these exact code additions to build the Inbound Webhook Router and Threading Engine.1. The Outbound Threading UtilityIn src/utils/email.service.js:Update sendTicketEmail to dynamically build the Reply-To header using the environment variable.Code: const replyTo = process.env.INBOUND_EMAIL_DOMAIN ? \reply+conv_${conversationId}@${process.env.INBOUND_EMAIL_DOMAIN}` : visitorEmail;`When making the API call (Mailgun/Resend/etc.), ensure this Reply-To header is attached.Generate a unique <uuid@omnicore.system> string to use as the outgoing Message-ID. Return this ID from the function so the controller can save it to the database for fallback threading.2. The Universal Inbound Webhook ControllerIn src/controllers/webhook.controller.js:Build route POST /api/webhooks/email/inbound.Step 1: Normalize Payload. Extract to, from, text (or Mailgun's stripped-text), and headers gracefully, accounting for Resend (JSON arrays), SendGrid (envelope JSON string), and Mailgun (form-data).Step 2: Threading Fast Path (UUID). Use regex to extract the conversation ID from the to address: const uuidMatch = toAddress.match(/reply\+conv_([a-zA-Z0-9\-]+)@/i);. If uuidMatch[1] exists, set conversationId = uuidMatch[1].Step 3: Threading Fallback (Headers). If the fast path fails, parse the In-Reply-To or References header. Query the database to find the conversation where last_email_message_id === extracted_header.Step 4: Database & Real-Time Sync. >   - If a valid conversationId is found, INSERT the normalized text into the messages table with sender_type = 'visitor'.Import your socket service and execute: io.to('tenant:' + tenant.id).emit('server:new_message', newMessage); to push the reply instantly to the dashboard.Step 5: Acknowledge. Immediately return res.status(200).send({ success: true }) to prevent the email provider from retrying the webhook.3. Database Schema UpdateEnsure the conversations table or messages table has a column (e.g., email_message_id VARCHAR) to store the generated Message-ID of the last outbound email sent to that visitor.In conversations.controller.js: When an agent replies and sendTicketEmail is fired, UPDATE the conversation record with the returned Message-ID so the webhook fallback can find it later.Execute these exact file creations and overwrites now.